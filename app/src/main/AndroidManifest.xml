<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hands_market">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"></uses-permission>

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hands_Market">
        <activity android:name=".StoreDetailActivity"></activity>
        <activity android:name=".GoodsRegisterActivity" />
        <activity android:name=".StoreRegistActivity" />
        <activity android:name=".ManagerActivity" />
        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="f577cb830a51d1e5cecceed9215c38e3" />
        <activity android:name=".MapViewActivity" />
        <activity android:name=".GoodsReviewActivity" />
        <activity android:name=".GoodsDetailActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


    </application>

</manifest>